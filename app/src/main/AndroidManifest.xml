<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.lab1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".presentation.menu.Menu"
            android:label="@string/title_activity_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".presentation.auth.AuthActivity"
            android:label="Auth"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".presentation.feedImage.ImageActivity"
            android:label="@string/title_activity_image_post"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name="com.project.lab1.presentation.notes.NotesActivity"
            android:label="@string/title_activity_see_notes"
            android:theme="@style/AppTheme.NoActionBar" />

<!--        <activity-->
<!--            android:name=".presentation.MainActivity"-->
<!--            android:label="@string/title_activity_main"-->
<!--            android:theme="@style/AppTheme.NoActionBar" />-->
        <activity
            android:name=".presentation.menu.NavigationActivity"
            android:label="@string/title_activity_navigate"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".presentation.feed.FeedActivity" />
        <activity android:name='.presentation.MainActivity'>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="android.intent.action.VIEW" /> <!-- Add for suppressing warning that app is not indexed by Google -->
            </intent-filter>
        </activity>
    </application>

</manifest>